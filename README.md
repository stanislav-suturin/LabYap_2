# LAB2
Ёмулируем протокол TokenRing. ¬о врем€ инициализации получаем от пользовател€ число и запускаем N потоков (goroutine).  аждый узел i должен быть св€зан очеред€ми (channel) с двум€ другими и получать данные от i-1 и посылать в i+1 узел. 

ќсновной поток должен иметь возможность отправить сообщение только одному узлу в виде экземпл€ра структуры/класса Token (data:string, recipient:int, ttl:int). ”злы передают токен по цепочке, пока сообщение не достигнет адресата сообщени€ или не истечет врем€ жизни (ttl-=1 на каждой пересылке).

Ѕазовый €зык - Go (golang). 
 
